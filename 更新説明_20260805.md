# 更新説明（2026-08-03 初修；08-04 續修；08-05 定稿）

對應論文返校件：《修訂標紅版 v8》（20260805）、《校樣直改版 v5》（20260805）、《圖表文字更正匯總》（20260805）。

《孔子衣鏡與早期孔子書寫——基於文本相似性的比較》（《嶺南學報》復刊第二十五輯）刊後在線更新。

本說明以**「改了什麼、爲什麼」**爲綱：每項先述**改動**，次述**緣由**，末述**影響邊界**（哪些數值／結論受影響、哪些不受影響）。文末爲「文件清單與對照」。

---

## 1. Ward 層次聚類：距離重定義爲 √(2(1−cos))

**改動**：小樣本層次聚類的輸入距離由 1−cos（餘弦不相似度）改爲 **dist(i,j) = √(2(1−cos(i,j)))**（弦距離，即 L2 歸一化 3-gram TF 向量之間的歐氏距離）；樹狀圖截斷取 **k=5**。修正版代碼爲本包 `small pipe 5.2.py`（首修）、`small pipe 5.3.py`（圖版修訂）與最終版 `small pipe 5.4.py`；正典運行輸出見本包 `experiment data(實驗數據)/202608041925＋small pipe 5.4＋…＋Lexical`。

**緣由**：SciPy 的 `linkage(method="ward")` 以「合併後組內方差最小化」爲目標，方差在歐氏空間中定義，**僅在歐氏度量下結果正確**；20260226 版 `small pipe 4.py` 直接以 1−cos 作輸入，1−cos 並非歐氏度量，屬距離定義誤用。對單位向量而言 √(2(1−cos)) 與餘弦相似度存在恆等關係，故修正在保持與原實驗完全相同的 TF 向量（重跑 TF 矩陣與 2026-02 原實驗逐位一致）之上，僅換用正確度量。

**影響邊界**：
- k=5 五簇簇內平均相似度依次爲：弟子傳簇 **0.1991**、孔子傳簇 **0.0496**、世家敘事簇 **0.0521**、《論語》語錄簇 **0.0531**、雜簇 **0.0452**（復算聚類報告 k=5 分群：群3 n=3、群4 n=4、群5 n=9、群2 n=11、群1 n=5）。
- k=2 劃分由 15/17 變爲 16/16，**唯一改隸的文本是《論語·泰伯》**；弟子簇（衣鏡·弟子傳＋史記·弟子列傳選＋家語·七十二弟子解選）與世家首尾各簇完全不動。
- **人文結論不變**：弟子傳間強聯繫、孔子傳間弱聯繫、《史記》樞紐地位等結論在正確度量下全部成立。刊面圖 5 以 v4 版替換（見第 7 條）。
- MDS 圖仍以 1−cos 作非度量相異度（MDS 不要求歐氏），未改動。

## 2. PCA：公式補中心化、改確定性求解、注②兩值更正

**改動**：（1）刊面 PCA 公式補中心化步驟——先作 **X̃ = X − x̄**（逐特徵減均值），再對 X̃ 作奇異值分解取前兩主成分；（2）代碼改 **svd_solver='full' 並固定隨機種子**；（3）刊面注②之兩值 **0.7223 更正爲 0.5528、0.6728 更正爲 0.6062**——《弟子列傳》（選）由「PCA 0.7223 第一」改爲「0.5528 第二」，《弟子傳》由「0.6728 第二」改爲「0.6062 第一」，二者名次互換；（4）解釋方差比（PC1 4.2%、PC2 3.8%）標於座標軸；圖內信息框僅保留其定義説明，不重複數值。

**緣由**：sklearn 之 PCA **必先中心化**，舊公式省略中心化步驟，與實際計算不符；20260226 版未固定種子且落入 randomized SVD 路徑，圖 3 座標**不可精確復現**，注②兩值即由此誤植。full SVD＋固定種子後輸出確定、可逐位復現。

**影響邊界**：僅圖 3 座標與注②兩值受影響。PCA 於本文爲輔助性可視化，相關人文結論另有網絡分析與聚類支撐，不受影響。刊面圖 3 以 v4 版替換（見第 7 條）；08-04 再以標籤避讓之 v5 版定稿（見第 11 條）。

## 3. Reranker：錯誤加載致隨機分類頭——刊面刪列，倉庫版正確實現

**改動**：刊面表 1 **刪除 Reranker 列**；本包 `pipeline21.2.py` 給出正確實現（Qwen3-Reranker 按官方模型卡以 CausalLM yes/no 打分，max_length 4096、suffix 保留式截斷；BGE 路徑補 `AutoModelForSequenceClassification` 導入）。

**緣由**：20260226 版兩套管線均以 `AutoModelForSequenceClassification` 加載 CausalLM 架構的 Qwen3-Reranker-8B，**分類頭爲隨機初始化**，reranker_logit 數值無效，不能刊佈。

**影響邊界**：Reranker 在設計上爲 **sidecar（旁路審計）——從不參與任何融合分數與排名**，故各報告之排名、融合分與全部結論均不受影響；僅表 1 一列與相關描述刪改。舊包原始運行記錄中的 reranker_logit 欄保持原貌、不作追改，判讀時以本條爲準。

## 4. 校勘閾值：norm_edit ≤ 0.35（0.32 非代碼所用值）

**改動**：表 3 之自動校勘閾值由刊面「0.32」勘正爲**歸一化編輯距離 norm_edit ≤ 0.35**。

**緣由**：**0.32 不存在於代碼中**；代碼實際判據爲 `detail_norm_edit_max = 0.35`，且閾值作用於歸一化編輯距離而非相似度。

**影響邊界**：純表述勘誤，不改任何計算；刊面表 3 與三份實驗報告修訂版已同步（見文末清單）。

## 5. 三處公式描述與代碼對齊：idf 平滑式、Coverage、robust_soft 仿射步

**改動與緣由**（均爲「刊面描述與代碼實現不一致」之對齊，計算本身未變）：
1. **idf 平滑式**：注①之 TF-IDF 定義勘正爲 sklearn 實際採用的平滑式 idf(t) = ln((1+n)/(1+df(t))) + 1，非教科書式 ln(n/df(t))。
2. **Coverage 定義**：勘正爲 **F1 × q_tf**（分段雙向最佳匹配之 F1，乘以共享三元組比例項 q_tf），與代碼一致。
3. **robust_soft 歸一化三步**：注⑤整段改寫爲代碼實際採用之三步——（1）分位數 min-max 標準化（H1 用 p10/p90，H2/Normal 用 p5/p95）；（2）分段 tanh 軟裁剪（H1 k=0.20，H2/Normal k=0.10）；（3）**仿射映射 (z̃+1)/3** 回 (0,1) 開區間。舊注⑤之 median/MAD＋½(1+tanh(αz)) 爲棄用舊公式。

**影響邊界**：三處均不改代碼、不改數值；刊面表 3 與注①③⑤已悉數納入。

## 6. 校勘記輸出原則：默認逐字位原始條目，「凡N見」合併改爲可選開關

**改動**：`pipeline21.2.py` 與 `small pipe 5.3.py` 的校勘記「凡N見」同項合併，改爲**可選參數 `merge_repeated_variants`，默認關閉**——默認輸出逐字位原始條目（replace 塊逐字 1:1 拆分、delete/insert 成句塊不拆、重出項不合併不計次）；開關開啓時方將同一（tag、A 段、B 段）合併爲一條並於計次 ≥2 時註「（凡N見）」。

**緣由**：**忠實機器輸出原則**——校勘記爲機器逐字位比對的原始證據層，逐位呈現最忠實、可逐條核對；「凡N見」合併屬呈現層之整理，宜由使用者顯式選擇，不宜作爲默認行爲。

**影響邊界**：dist／rep／ins／dele 與 norm_edit 等一切計數在開關前後**逐值不變**（仍按字符位逐位計），開關僅影響展示行。兩份校勘圖表修訂 docx（自動校勘图表 總、CDM 自动校勘图表_14对）爲人工整理之呈現層文檔，按「凡N見」合併體例修訂（改動標紅），與代碼默認的逐字位原始輸出並行不悖。復測（表 5 兩對嚴式文本，以 `small pipe 5.4.py` 分別取 `merge_repeated_variants=False/True` 各跑一次）：默認逐字位 19／10 條，開關開啓 17／9 條（含「杚作紇（凡2見）」等），計數兩態一致。

## 7. 圖版處置（圖 2 保留原版；圖 3／4／5 v4 替換，圖 3 後定於 v5）與表 3 改排

**改動**：刊面圖 3／4／5 以 `small pipe 5.3.py` 生成之 v4 版替換（其中圖 3 續以 `small pipe 5.4.py` 之 v5 版定稿，見第 11 條），見本包 `figure (論文圖表）`；表 3 改排爲單表（解釋文字移作當頁腳注），刊面爲 Word 內置表格，本包不出圖版；圖 2 不入本更新包（見下）。

**緣由**（逐圖）：
- **圖 2（網絡圖）**：**保留原 ctext 圖版（作者裁定）**，不以 5.3 生成版替換；該圖由 ctext 線上工具生成，刊面節點文字一仍其舊，本次不作更正。
- **圖 3（PCA）**：改中心化＋full SVD＋固定種子之確定解；説明框放寬（note_box_width_scale 0.70→1.0）並折行修溢框；説明框末之投影質量套語刪除——5.3 初版即以實測解釋方差替換「合計>50% 爲良好投影」模板句，08-04 再刪「投影僅作輔助觀察」句；解釋方差比仍標於座標軸；**標籤修復**——最右緣 12% 區間的點標籤改畫在點左側並加寬右邊距，修「衣鏡·弟子傳」等長標籤衝出坐標框。
- **圖 4（Strength 中心性）**：公式改 mathtext 標準排版（與刊 234 注⑥統一爲 cos_raw、j≠i）；排名與數值不變。
- **圖 5（Ward 樹狀圖）**：距離改 √(2(1−cos))、k=5 截斷；標題去「（歐氏距離，L2 歸一化）」括注（距離定義仍在圖內説明框）；去除 k=6 截斷綫與圖例，僅存 k=2、k=5（k=6 未用於本文任何結論，並列三綫易誤導讀者以爲 k=6 亦爲分析口徑）。樹形、合併距離與 k=5 分群結果一字未變。
- **表 3（改排）**：上下兩表併爲一表，解釋文字移作當頁腳注；norm_edit ≤ 0.35、robust_soft 三步（含仿射步）、idf 平滑式諸勘誤隨腳注同步。
- 諸圖及報告文字「為」→「爲」（正字統一）、標籤繁體歸一。

**影響邊界**：v4 相對 v3 僅圖版文字修訂，數值零改動。

## 8. 小樣本代碼沿革：5 → 5.1 → 5.2 → 5.3 → 5.4

| 版本 | 日期 | 改動 | 數值影響 |
|---|---|---|---|
| small pipe 5 | 2026-08-01 | Ward 距離 1−cos → √(2(1−cos))；k 截斷線 2/4/6 → 2/5/6；聚類報告同步 | 見第 1 條（僅泰伯改隸） |
| small pipe 5.1 | 2026-08-01 | 圖內説明框公式改 matplotlib mathtext 標準排版（Ward／MDS／PCA／Strength 四處） | 零改動 |
| small pipe 5.2 | 2026-08-02 | 顯示標籤簡→繁歸一（正文語料一字未動）；《論語》篇名規範形改繁體。與 5.1 逐行 diff，除標籤與時間戳外零差異 | 零改動（五值 0.1991/0.0496/0.0521/0.0531/0.0452 不變） |
| small pipe 5.3 | 2026-08-02 → 08-04 | 初版：校勘記「凡N見」合併；圖版文字修訂（第 7 條）。**08-03 兩筆**：（1）PCA 説明框放寬折行＋右緣長標籤左置、網絡圖圖例左下→左上（第 7 條）；（2）「凡N見」合併改爲可選開關、**默認關閉**（默認逐字位原始條目，第 6 條）。**08-04 一筆**：PCA 説明框刪投影質量套語 | 零改動（5.3 運行 202608041704 之 Excel 六表與 5.2 運行 202608021346 逐值零差異） |
| small pipe 5.4（**最終版**） | 2026-08-04 | PCA 標籤避讓 `_pca_relax_labels()`（第 11 條）；本包正典運行 202608041925 即由此版產出 | 零改動（Excel 六表與 5.3、5.2 兩次運行逐值零差異） |

PCA 之 full SVD＋固定種子修復在 5 系基線中一併落實（詳第 2 條）。本包收錄 5.2、5.3（沿革留檔）與 5.4（最終版）；5／5.1 爲中間工作版，不另發佈。主管線沿革：pipeline20（20260226）→ 21（刊面口徑）→ 21.1（Reranker 工程修復）→ **21.2**（本包，校勘記開關版）。

## 9. 運行環境

python **3.13**；依賴凍結見 `code pipe(實驗代碼）/requirements_py313_freeze.txt`，配置步驟見同目錄 `环境配置说明_20260802.md`。

---

## 10. 文件清單與對照

本包爲**瘦身包**：只收錄受本次勘誤影響而更新的文件，不再保留與 20260226 包同構的空鏡像目錄（text data、CDM data、數據統計）；未收錄材料一律沿用 20260226 包（見倉庫對應 tag／發佈）。

| 本包文件 | 20260226 包對應 | 差異說明 |
|---|---|---|
| `README.md`、`更新説明_20260804.md` | `README.md` | 本次更新總說明 |
| `Supplementary Materials Guide(材料說明).docx` | 同名 | 2026-08-05 修訂版（改動標紅）：條目更新爲 21.2／5.2／5.3／5.4，圖 3 作 v5、圖 4／5 作 v4；實驗數據改指 5.4 正典運行 202608041925（與 202608041704、202608021346 諸次運行數值逐值零差異）；Ward、PCA、Reranker、norm_edit 諸勘誤同步 |
| `CDM_DICT_3.1(《孔子衣鏡》字典映射）.docx / .pdf` | `CDM_DICT` 舊版 | **嚴式判據 v3.1**：嚴式異文判定所依據之字典映射（docx＋pdf 兩檔） |
| `code pipe(實驗代碼）/pipeline21.2.py` | `pipeline20.py` | Reranker 官方 CausalLM yes/no 打分（修復隨機分類頭）；BGE 導入修復；suffix 保留式截斷＋max_length 4096；校勘記「A有B无」表述；「凡N見」合併改可選開關（默認逐字位，第 6 條） |
| `code pipe(實驗代碼）/small pipe 5.4.py` | `small pipe 4.py` | **最終版**：5.3＋PCA 標籤避讓（第 11 條），數值零改動
| `code pipe(實驗代碼）/small pipe 5.3.py` | `small pipe 4.py` | 沿革留檔：Ward／PCA 修正＋圖版文字修訂＋校勘記開關（默認逐字位）；算法與數值相對 5.2 零改動 |
| `code pipe(實驗代碼）/small pipe 5.2.py` | `small pipe 4.py` | 沿革留檔（第 8 條）：Ward 歐氏度量、PCA 確定性、mathtext、繁體標籤 |
| `code pipe(實驗代碼）/run_small_pipe54_headless.py` | （無） | 無頭運行器（最終版；本包正典運行 202608041925 由此產出） |
| `code pipe(實驗代碼）/run_small_pipe53_headless.py`、`run_small_pipe52_headless.py` | （無） | 無頭運行器（沿革留檔） |
| `code pipe(實驗代碼）/requirements_py313_freeze.txt`、`环境配置说明_20260802.md` | （無） | python3.13 依賴凍結與環境說明 |
| `figure (論文圖表）/圖3_替換v5_*.png`、`圖4／圖5_替換v4_*.png` | `03～05-圖3～圖5 *.png` | 圖 3 爲 5.4 生成之 v5（標籤避讓），圖 4／圖 5 爲 v4；三檔均取自正典運行 202608041925 之輸出，與該運行同名 png 逐位元相同。逐圖緣由見第 7 條；圖 2 保留原 ctext 圖版（作者裁定），本次不作更正，不入本包。表 3 改排爲刊面 Word 內置表格，不出圖版（第 7 條） |
| `experiment data(實驗數據)/202608041925＋small pipe 5.4＋…＋Lexical/` | `202602162033＋…`（字面部分） | 5.4 終版正典運行全套輸出，共 15 檔（Excel 六表、9 張圖、主結果報告、聚類與中心性分析報告、成對相似度權重表、預處理監測、運行日誌）；TF 矩陣與 2026-02 原實驗逐位一致，Excel 六表與 202608041704（5.3）、202608021346（5.2）逐值零差異 |
| `data analysis report …/實驗報告/文本層次最優策略分析報告（融合H2）.docx` | 同名 | 修訂版（標紅）：threshold=0.32 勘正爲 norm_edit ≤ 0.35 |
| `data analysis report …/實驗報告/大規模實驗報告.docx` | 同名 | 修訂版（標紅）：校勘閾值勘正（兩處）；§2.6 補 Reranker 勘誤說明 |
| `data analysis report …/實驗報告/自動校勘图表 總.docx` | 同名 | 修訂版（標紅）：89 表 274 條重複校勘記按「凡N見」體例合併（187 處標註）、條目重編號（呈現層整理，見第 6 條） |
| `data analysis report …/實驗報告/CDM 自动校勘图表_14对.docx` | 同名 | 修訂版（標紅）：3 表 5 條合併（4 處標註）、條目重編號 |
| `.gitignore`、`.gitattributes` | 同名 | 沿用（xlsx/json/npy 走 LFS） |

**未收錄（沿用 20260226 包）**：text data（文本語料）、CDM data（元數據）、data analysis report 之數據統計全部文件、未受影響之《Qwen3 語義模型對照實驗報告》《小樣本對照报告_32篇vs26篇》，以及 20260226 版全部原始實驗數據目錄（其中舊版 reranker_logit 欄數值無效，判讀以第 3 條爲準；原始運行記錄保持原貌，不作追改）。

## 11. 圖 3（PCA）標籤避讓：small pipe 5.4

**改動**：PCA 散點圖左側密集區的文本標籤此前相互壓印（600 dpi 原件即不可辨讀）。`small pipe 5.4.py` 新增 `_pca_relax_labels()`：以首次量得的文字框尺寸為準，按固定候選方位（原位→下方→左側→更遠上下）作**確定性貪心避讓**，並限制標籤不越出座標框；不擁擠處保持原位，故右側佈局與 5.3 完全一致。避讓在座標範圍、載荷箭頭、圖例全部就位之後執行——若在箭頭之前執行，後續自動縮放會推移座標而使結果失效。

**緣由**：付印縮版後壓印更甚，影響可讀性。

**影響邊界**：**零數值改動**。5.4 正典運行（202608041925，本包所收）與 5.3 運行（202608041704，未隨包發佈）之 Excel 六表逐值一致（pairs_total、matrix_cos_raw、matrix_tfidf、matrix_jaccard、matrix_embed、centrality）；僅圖 3 的標籤位置變化。替換件更新爲 `圖3_替換v5_PCA（標籤避讓·繁體標籤）.png`。
